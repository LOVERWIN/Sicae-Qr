package com.sistema.repositorio;

import com.sistema.modelo.ConfigChecador;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IConfigChecadorRepositorio extends JpaRepository<ConfigChecador,Integer> {
}
